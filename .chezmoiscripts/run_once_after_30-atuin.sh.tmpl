#!/usr/bin/env bash
set -euo pipefail

source "{{ .chezmoi.homeDir }}/.bashrc"

fnox exec -- atuin login --username $ATUIN_USERNAME --password $ATUIN_PASSWORD --key $ATUIN_KEY

echo "✅ Successfully signed into atuin!"
